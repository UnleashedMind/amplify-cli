#error: add "s" for searchPosts
#error: either AppSync or Elasticsearch has an error, the query does not work
query SearchPosts {
  searchPosts(filter: { title: { wildcard: "S*Elasticsearch!" }}) {
    items {
      id
      title
    }
  }
}
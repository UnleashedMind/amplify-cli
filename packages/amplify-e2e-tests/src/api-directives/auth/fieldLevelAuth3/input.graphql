#change: changed "Admin" to "admin"
#error: there is no "username" field, which is required as teh ownerfield
#change: change "email" to "username"
type Employee @model {
  id: ID!
  username: String

  # Owners & members of the "Admin" group may read employee salaries.
  # Only members of the "Admin" group may create an employee with a salary
  # or update a salary.
  salary: String
    @auth(rules: [
      { allow: owner, ownerField: "username", operations: [read] },
      { allow: groups, groups: ["admin"], operations: [create, update, read] }
    ])
}

##fieldLevelAuth3
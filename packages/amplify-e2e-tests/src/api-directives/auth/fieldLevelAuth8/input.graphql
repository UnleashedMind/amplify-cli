#change: "Admins" to "admin"
type Employee @model
  @auth(rules: [
	  { allow: owner },
	  { allow: groups, groups: ["admin"] }
  ]) {
	id: ID!
	name: String!
	address: String!
	ssn: String @auth(rules: [{allow: owner}])
}

##fieldLevelAuth8
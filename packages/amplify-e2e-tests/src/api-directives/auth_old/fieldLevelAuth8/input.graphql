#change: "Admin" to "admin"
type Employee @model
  @auth(rules: [
	  { allow: owner },
	  { allow: groups, groups: ["admins"] }
  ]) {
	id: ID!
	name: String!
	address: String!
	ssn: String @auth(rules: [{allow: owner}])
}

##fieldLevelAuth8